# Expo public env (EXPO_PUBLIC_*) — safe to ship in the client bundle.
# Without the Clerk key the app runs in local demo mode (sample portfolio, no auth).

EXPO_PUBLIC_API_URL="https://folio-fawn-nu.vercel.app"
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=""
